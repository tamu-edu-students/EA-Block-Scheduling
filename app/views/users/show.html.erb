<%= render @user %>

<div class="edit-actions action-container">
  <%= link_to "Edit User", edit_user_path(@user), class: "btn btn--primary" %>
  <%= link_to "Back to Users", users_path, class: "btn btn--primary" %>
</div>
